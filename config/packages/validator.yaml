framework:
    validation:
        enabled: true
        # Enables validator auto-mapping support.
        # For instance, basic validation constraints will be inferred from Doctrine's metadata.
        #auto_mapping:
        #    App\Entity\: []

when@test:
    framework:
           validation:
                 enabled: true
                 mapping:
                    paths: ['%kernel.project_dir%/src/Entity'] # Path to entity files
